import { Meta, Title, Controls, Canvas } from '@storybook/blocks';
import * as PasswordStories from './password.stories';

<Meta of={PasswordStories} />
<Title of={PasswordStories} />

Password Input is used to capture sensitive information that the user wouldn’t want to be visible on the screen.

<Canvas of={PasswordStories.Preview} />
<Controls of={PasswordStories.Preview} />
