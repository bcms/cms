import { Meta, Title, Controls, Canvas } from '@storybook/blocks';
import * as CheckboxStories from './checkbox.stories';

<Meta of={CheckboxStories} />
<Title of={CheckboxStories} />

Checkbox is used as a way to handle boolean-like values.

<Canvas of={CheckboxStories.Preview} />
<Controls of={CheckboxStories.Preview} />
