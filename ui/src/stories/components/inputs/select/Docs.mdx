import { Meta, Title, Controls, Canvas } from '@storybook/blocks';
import * as SelectStories from './select.stories';

<Meta of={SelectStories} />
<Title of={SelectStories} />

Select is used to show a list of options from the dropdown.

<Canvas of={SelectStories.Preview} />
<Controls of={SelectStories.Preview} />
