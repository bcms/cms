import { Meta, Title, Controls, Canvas } from '@storybook/blocks';
import * as DateStories from './date.stories';

<Meta of={DateStories} />
<Title of={DateStories} />

Date Input is used to select a date from the calendar.

<Canvas of={DateStories.Preview} />
<Controls of={DateStories.Preview} />
