import { Meta, Title, Controls, Canvas } from '@storybook/blocks';
import * as ToggleStories from './toggle.stories';

<Meta of={ToggleStories} />
<Title of={ToggleStories} />

Toggle is used as a way to handle boolean-like values.

<Canvas of={ToggleStories.Preview} />
<Controls of={ToggleStories.Preview} />
