import { Meta, Title, Controls, Canvas } from '@storybook/blocks';
import * as MultiAddStories from './multi-add.stories';

<Meta of={MultiAddStories} />
<Title of={MultiAddStories} />

Multi Add is used to define a set of named constants which you can choose from.

<Canvas of={MultiAddStories.Preview} />
<Controls of={MultiAddStories.Preview} />
