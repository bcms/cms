import { Meta, Title, Controls, Canvas } from '@storybook/blocks';
import * as TextStories from './text.stories';

<Meta of={TextStories} />
<Title of={TextStories} />

Text Input is used to capture freeform text from the user.

<Canvas of={TextStories.Preview} />
<Controls of={TextStories.Preview} />
