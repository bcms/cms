import { Meta, Title, Controls, Canvas } from '@storybook/blocks';
import * as TextAreaStories from './text-area.stories';

<Meta of={TextAreaStories} />
<Title of={TextAreaStories} />

Textarea Input is used to capture freeform text from the user.

<Canvas of={TextAreaStories.Preview} />
<Controls of={TextAreaStories.Preview} />
