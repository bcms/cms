import { Meta, Title, Controls, Canvas } from '@storybook/blocks';
import * as ColorStories from './color.stories';

<Meta of={ColorStories} />
<Title of={ColorStories} />

Color Picker allows users to select a color from a predefined set of colors or a custom one.

<Canvas of={ColorStories.Preview} />
<Controls of={ColorStories.Preview} />
