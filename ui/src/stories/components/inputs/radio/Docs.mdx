import { Meta, Title, Controls, Canvas } from '@storybook/blocks';
import * as RadioStories from './radio.stories';

<Meta of={RadioStories} />
<Title of={RadioStories} />

Radio is used to select a single option from a list of two or more mutually exclusive options.

<Canvas of={RadioStories.Preview} />
<Controls of={RadioStories.Preview} />
