import { Meta, Title, Controls, Canvas } from '@storybook/blocks';
import * as NotificationStories from './notification.stories';

<Meta of={NotificationStories} />
<Title of={NotificationStories} />

Notification is used to notify the user of change / error / etc. from within
the app.

Notifications are triggered using the <code class="text-green">window.bcms.notification</code> service.

Examples for a success notification:

```js
window.bcms.notification.success('Notification message');
```

<Canvas of={NotificationStories.Preview} />
<Controls of={NotificationStories.Preview} />
