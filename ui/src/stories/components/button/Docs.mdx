import { Meta, Title, Controls, Canvas } from '@storybook/blocks';
import * as ButtonStories from './button.stories';

<Meta of={ButtonStories} />
<Title of={ButtonStories} />

<p>
  Button component is used as a trigger for an event or action, or as a link.
</p>
<p class="mb-10">
  Label of the button is set through a default slot which can be text or HTML.
</p>

<Canvas of={ButtonStories.Preview} />
<Controls of={ButtonStories.Preview} />
